# Format code shift+alt+f  sort imports
import sys
from datetime import datetime
from tkinter import *

import pandas as pd
import sqlalchemy
from sqlalchemy import create_engine

print(sys.version)
print(sys.executable)


# root = Tk(className=' PCS Indu Status Checker ')
# entry = Entry(root,bd=3, width=15)
